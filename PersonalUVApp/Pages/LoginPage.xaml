<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:PersonalUVApp.Controls"
             xmlns:converters="clr-namespace:PersonalUVApp.Converters"
             x:Class="PersonalUVApp.Pages.LoginPage" 
             Title="Login" BackgroundColor="{StaticResource PageBackgroundColor}">
    
    <ContentPage.Content>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="4*" />
            </Grid.RowDefinitions>
            <controls:MyTableView Intent="Form" Margin="10,0" Grid.Row="1" VerticalOptions="CenterAndExpand" >
                <TableRoot>
                    <TableSection>
                        <EntryCell Label="Username:" Placeholder="Enter your name"
                                   Text="{Binding UserName}" />
                        <EntryCell Label="Password:" Placeholder="Enter your password" />
                        <SwitchCell Text="Remember Me" On="true" />
                    </TableSection>
                    <TableSection> 
                        <ViewCell>
                            <Button Text="Login" HorizontalOptions="Fill" TextColor="White" 
                                    BackgroundColor="{StaticResource ButtonBackgroundColor}" />
                        </ViewCell>
                    </TableSection>
                    <TableSection >
                        <ViewCell>
                            <StackLayout>
                                <controls:UnderLineLabel Text="Forget Password" LineColor="Black" HorizontalOptions="Center" />
                                <Label Text="{Binding UserName, Mode=TwoWay}"
                                       BackgroundColor="{Binding UserName, Mode=TwoWay, Converter={StaticResource NameToColorConverter}}" /> 
                            </StackLayout>
                        </ViewCell>
                    </TableSection>
                </TableRoot>
            </controls:MyTableView>
        </Grid>
        
        
        
    
    </ContentPage.Content>
</ContentPage>